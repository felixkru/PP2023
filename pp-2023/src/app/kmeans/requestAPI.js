'use client'

import {dataObjectsForRequest} from '../utils/request-objects';

const apiRequest = (url, data, options) => {
    const result = async function () {
        try {
            //const response = await fetch(url),
        } catch (err) {
            throw new Error(err);
        }
    }
}

export const exportFunctionDataForKMeans = () => {
    return (
        apiRequest()
    )
};